<%= form_for @meeting do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :date_time %>
  <%= f.text_field :date_time %><br>

  <%= f.label "Choose Creator" %>
  <%#  FIX THIS %>
  <%= f.collection_select @meeting.user_id, @users, :id, :first_name %>
  <%= f.submit %>
<% end %>
